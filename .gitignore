# 为什么需要 `.gitignore`：
# 因为工程项目里会产生很多“不应该提交到 Git”的本地文件。
# 如果不忽略它们，仓库会很快混入环境文件、缓存文件、系统文件和构建产物。
#
# 为什么这对 AI Agent 项目也重要：
# 后续这类项目通常会接入更多本地配置、日志、缓存和索引文件。
# 提前把忽略规则建立好，能避免后面仓库越来越乱。

# Python 虚拟环境
.venv/
venv/

# Python 缓存
__pycache__/
*.py[cod]

# 环境变量文件
.env

# macOS 系统文件
.DS_Store

# 测试与覆盖率缓存
.pytest_cache/
.coverage
htmlcov/
