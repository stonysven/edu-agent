"""
为什么需要这个文件：
`api` 目录专门存放对外暴露的 HTTP 接口。
把接口层单独放出来，可以让“外部请求处理”和“内部业务逻辑”分开，代码会更清楚。

这个文件做了什么：
当前文件只负责把 `app/api` 声明为 Python 包。

模块职责说明：
- `api` 负责接收请求、校验参数、返回响应
- `agents` 负责具体智能体逻辑
- `orchestrator` 负责协调多个步骤或多个智能体
- `memory` 负责记忆管理
- `rag` 负责检索增强生成
- `tools` 负责外部工具调用
"""
