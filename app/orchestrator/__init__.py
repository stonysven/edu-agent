"""
为什么需要 `orchestrator`：
当系统变复杂后，一个请求往往不是一步完成的。
例如：
1. 先理解用户问题
2. 再决定是否检索知识库
3. 然后决定是否读取记忆
4. 最后调用某个 Agent 生成答案

如果把这些步骤都写在接口层或 Agent 层，会越来越乱。
所以我们预留一个“编排层”来负责串联流程。

这个文件做了什么：
当前只把 `orchestrator` 声明为包，方便后续逐步扩展。
"""
