"""
为什么需要 `rag`：
RAG 是 Retrieval-Augmented Generation 的缩写，中文常叫“检索增强生成”。
它的核心思想是：
先从外部知识中检索相关内容，再把检索结果交给模型生成答案。

为什么这么做：
因为大模型的参数知识并不总是最新、最全、最准确。
如果系统要回答课程知识、企业文档或业务资料，单靠模型记忆往往不够。

这个文件做了什么：
当前只声明 `rag` 是一个包，后续可以加入：
- 文档切分
- 向量化
- 检索器
- 重排器
- 上下文拼接器

为什么先不放复杂实现：
RAG 的完整链路较长，骨架阶段先明确位置和职责，
会比直接塞进大量向量库和嵌入代码更适合学习。
"""
