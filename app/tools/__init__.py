"""
为什么需要 `tools`：
大模型本身主要擅长语言理解与生成，但它并不能天然访问外部世界。
例如：
- 查询天气
- 读取数据库
- 调用内部服务
- 执行搜索

这些能力通常以“工具”的形式接入。

概念解释：
Tool 调用可以理解为“让 Agent 借助手脚做事”。
模型负责决定“什么时候调用什么工具”，
工具负责真正执行动作并返回结果。

这个文件做了什么：
当前只声明 `tools` 是一个包，后续会放具体工具适配器。
"""
