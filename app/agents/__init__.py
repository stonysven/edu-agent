"""
为什么单独保留 `agents` 目录：
在 AI Agent 系统中，“Agent”通常表示能够理解目标、调用工具、执行步骤的智能体。
把 Agent 独立成一层，后续就可以很自然地放入：
- 单智能体实现
- 多角色智能体实现
- 不同领域的专用智能体

这个文件做了什么：
当前只声明 `agents` 是一个包，不放具体代码。

概念解释：
Agent 不是简单的“调用一次大模型”。
它更像一个“会思考、会选择动作、会串联步骤的执行单元”。
后续我们可以在这里加入：
- Agent 输入输出定义
- Agent 执行器
- Agent 提示词模板
"""
