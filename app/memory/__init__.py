"""
为什么需要 `memory`：
AI Agent 如果完全没有记忆，每次请求都像第一次见到用户，
这会导致上下文断裂、无法持续对话、无法长期记录偏好。

概念解释：
- 短期记忆：通常是当前会话里的上下文，比如最近几轮对话
- 长期记忆：通常是跨会话保留的信息，比如用户偏好、重要事实

这个文件做了什么：
当前只声明 `memory` 是一个包。

为什么先不实现复杂记忆系统：
真正的记忆系统通常会涉及数据库、向量库、召回策略与清洗策略。
对于教学级骨架，先把目录职责划清楚，比一开始引入复杂实现更容易理解。
"""
