"""
为什么需要这个文件：
Python 只有在看到 `__init__.py` 时，才会明确把 `app` 目录识别为一个包。
这样做的好处是，我们后续可以使用 `from app.xxx import yyy` 这种清晰的导入方式。

这个文件做了什么：
当前文件本身不放业务逻辑，只承担“声明 app 是一个 Python 包”的作用。

为什么这里保持简单：
这里没有写初始化代码，是因为项目骨架阶段更适合把副作用代码放在明确的位置，
例如 `app/main.py` 或 `app/core/config.py`，这样更容易理解和排查问题。
"""
